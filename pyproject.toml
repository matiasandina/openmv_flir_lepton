[project]
name = "openmv-flir-lepton"
version = "0.1.0"
description = "Host control tools for OpenMV H7 FLIR Lepton recording."
requires-python = ">=3.10"
dependencies = [
    "pyserial>=3.5",
]

[tool.uv]
package = false
